events {}
http {
        server {
                listen 80 default_server;
                root /var/www;
                index index.html index.html;
		include /etc/nginx/mime.types;
                location / {
                try_files $uri $uri/ /;
                }
		location /cookbook{
			proxy_pass http://35.176.224.39:9000;
		}
        }
}
